{
  "dependencies": {
    "axios": "^1.4.0",
    "json-server": "^0.17.3",
    "json-server-auth": "^2.1.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db": "json-server db.json -m ./node_modules/json-server-auth"
  }
}
